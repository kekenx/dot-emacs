;;; my-markdown.el --- markdown configuration
;;; Commentary:
;;; markdown configuration.

;;; Code:

;; (ensure-package-installed 'markdown-mode 'markdown)
;; (add-to-list 'auto-mode-alist '("\\.md\\'" . markdown

;;; my-markdown.el ends here
